use crate::frontend::*;
use linked_syntax_tree::SyntaxTree;

pub fn gen(_tree: SyntaxTree<Statement>) {
    // let mut cursor = tree.cursor_mut();
}

#[cfg(test)]
mod tests {
    #[test]
    fn back_one() {

    }
}
