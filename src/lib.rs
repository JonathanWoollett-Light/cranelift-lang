/// The backend that converts the AST to crane lift.
pub mod backend;
/// The frontend that converts text to the AST.
pub mod frontend;
/// The middle component that runs compile time evaluation and optimizations on the AST.
pub mod middle;
